{
  "$schema": "https://agent-plugins.org/schemas/v1.0.0/mcp.json",
  "mcpServers": {
    "strict-goal": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/server/main.js",
        "--data-dir",
        "${PLUGIN_DATA}"
      ],
      "env": {
        "STRICT_GOAL_ROOT": "${PLUGIN_ROOT}",
        "STRICT_GOAL_DATA": "${PLUGIN_DATA}",
        "STRICT_GOAL_LOG": "warn"
      },
      "cwd": "${PLUGIN_DATA}"
    }
  }
}
