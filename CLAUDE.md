<!-- knowledge-kit:begin section=general-instructions version=1.10.10 -->
## Communication Style
- Use Caveman mode.
- Drop filler words, preambles, and recaps.
- Be extremely concise. Keep explanations under 2 sentences.
- Full code, minimal chatter.

## プランの保存場所
planモードで作成したマークダウンファイルは、<プロジェクトルート>docs/plans 以下に保存する。

## 返信の言語
最終的な返信は日本語で出力する。

## コード検索の指示方針
- コードベースの機能調査やコード探索を行う際は、最初に MCP ツール `search` (BM25 Code Search) を優先して使用してください。
- **Claude Code での呼び出し手順**: Claude Code では MCP ツールが Deferred Tool となるため、初回呼び出し前に必ず `ToolSearch` (`select:mcp__bm25-code-search__search`) でスキーマをロードしてから `mcp__bm25-code-search__search` を実行してください。
- `search` で結果が得られない場合、または特定のシンボル名の完全一致を直接検索する場合にのみ `grep_search` や `glob` を使用してください。
<!-- knowledge-kit:end section=general-instructions -->

<!-- knowledge-kit:begin section=recording-rules version=1.10.10 -->
## 知見の記録ルール

**作業中に重要な知見を発見したら、ユーザーの指示を待たず自動で記録すること。**

| 種類 | 記録先 | 例 |
|---|---|---|
| また踏むバグ・罠 | `CLAUDE.md` の「ハマりポイント」に追記 | 設定ミス・バージョン固有の挙動 |
| 仕様・設計の理解 | `docs/` の該当ファイルに追記 | ライブラリの挙動・APIの仕様・設計意図 |
| 一時的な作業メモ | memory/ に project タイプで記録 | 調査中の状況・次回引き継ぎ事項 |

記録したら「〇〇をハマりポイントに追記しました」と一言報告する。
<!-- knowledge-kit:end section=recording-rules -->

<!-- knowledge-kit:begin section=gotchas version=1.10.10 -->
## ハマりポイント

<!--
書き方のルール（このコメントは残してよい）:
- 1項目 = 1箇条書き。先頭を **太字** で「何が起きるか／何に注意するか」を要約する
- 対処法・原因まで1項目内に書き切る。長くなる場合は docs/ に本文を移し、
  「（詳細: `@docs/xxx.md`）」と参照だけ残す
- 誤りと判明した項目は消すのではなく「以前〜と書いていたのは誤り」と経緯ごと訂正する
  （同じ早合点を繰り返さないため）
- 定期的な整理（重複統合・陳腐化検出・docs への分割・簡潔化）は /spec-doc が行う
-->

- （ここに知見を追記していく）
- **Git Bash の `/tmp` と Windows Python の `/tmp` は別物**: bash のヒアドキュメントで `/tmp/x` に書いたファイルを `python -c "open('/tmp/x')"` で読むと `FileNotFoundError`（bash は msys の /tmp、Python はカレントドライブ直下の `D:\tmp` を見る）。一時ファイルは scratchpad の絶対パスで統一するか、bash と Python のどちらか一方だけで完結させる。
- **文字列置換パッチのアンカーは前方一致で二重マッチする**: `"**エラー条件**: \`E_SESSION_NOT_FOUND\` / \`E_INTERNAL\`"` のような短いアンカーは、同じ書き出しの別節にも当たって `MATCH 2` で落ちる。しかも**先に別の置換で末尾を伸ばした結果あとから衝突する**ので、置換順に依存する。アンカーは行末まで（句点込みで）取る。
- **Bash の長文ヒアドキュメントは失敗しうる**: 数百行規模の日本語＋コードフェンス混在ブロックを `cat >> file <<'EOF'` で流すと `unexpected EOF while looking for matching` で落ちることがある（原因は入力の途中切断で、引用符やクォート形式の問題ではない）。回避策は Write ツールで scratchpad にチャンクを書き、`cat chunk >> target` で連結する。連結後は `grep -n '^## '` で節が全部入ったか必ず確認する（1チャンク取りこぼしても静かに成功するため）。
- **`grep -o 'E_[A-Z0-9_]*'` はエラーコード列挙で偽陽性を出す**: 設計書のエラーコードを数えるとき、この正規表現は `XDG_STATE_HOME` を `E_HOME`、`CLAUDE_PLUGIN_DATA` を `E_PLUGIN_DATA`、`CLAUDE_PLUGIN_ROOT` を `E_PLUGIN_ROOT` として拾う（部分一致するため）。45件出て「設計書の42件と合わない」となる。`\bE_[A-Z][A-Z0-9_]*\b` のように語境界を付けるか、拾った候補を1件ずつ `grep -n` で出現箇所確認すること。
- **`node --test test/`（末尾スラッシュ付きディレクトリ）は Node 24 で `MODULE_NOT_FOUND` になる**: ディレクトリを渡したいなら `node --test` だけで実行する（`test/` 配下を自動探索する）か `node --test 'test/*.test.js'` のようにグロブを渡す。末尾スラッシュ付きの生パスは CommonJS require 解決に落ちて壊れる。（訂正・追記）末尾スラッシュの有無は無関係で、`node --test rubric-loop/server/test`（スラッシュ無し）でも同じ `MODULE_NOT_FOUND` になることを Phase 3 セッション実行（design→plan→implement 連鎖の `score_submit` 用コマンドエビデンス生成）で確認した。`--test` にディレクトリの生パスを渡すこと自体がアウトで、パス無し(自動探索)かファイル単体パスかグロブのみが安全。`execFileSync` でコマンド文字列を機械的に分解して再実行するような自作スクリプトでは、ディレクトリを指す文字列だけ特別扱い（`cwd` をそのディレクトリにして `--test` のみ渡す）が要る。
- **T012 で抽出した `schemas/tools.json` の `score_submit.scores[].criterion_id` パターンと `weakness` の制約が設計書と矛盾していた**: (1) パターンが `^[a-z0-9][a-z0-9-]{1,63}$`（ハイフンのみ）になっていたが、実際の rubric preset（`presets/design.json` の `failure_mode_mapping` 等）や `rubric/schema.js` の criterion id はアンダースコアを使う。`^[a-z0-9][a-z0-9_-]{1,63}$` に修正（T035）。(2) `weakness` に `minLength:10` が付いていたが、設計書は「score=10 のときのみ `'none'`（4文字）を許す」としており矛盾。共有 `validate()` は oneOf/条件分岐を持たないため、スキーマ側の `minLength` は外し、`assertWeaknessValid()`（`score_submit.js`）で「`'none'` なら score===10 必須、それ以外は手動で `WEAKNESS_MIN_LENGTH` 以上を要求」を実装した。設計書の schema 例をそのまま抽出するだけでは通らないケースがある（T028/T031 の「$schema 等の未サポートキーを削る」だけでなく、値レベルの制約矛盾もチェックが要る）。
- **T039 の plan 記述（実装計画の受け入れ基準）が実際の `schemas/tools.json`（設計書§6.4.5から抽出）と矛盾していた**: `rubric_amend` の入力スキーマは `additionalProperties:false` で `policy` フィールドを一切受け付けない（常に前版の policy を継承する仕様）。設計書自身のエラー条件表も「`E_THRESHOLD_IMMUTABLE` はこの経路では構造的に到達不能」と明記している。にもかかわらず plan の T039 受け入れ基準は「pass_score の変更が E_THRESHOLD_IMMUTABLE になること」「change_note が19文字で E_VALIDATION、20文字で通ること」と書いており、(a) 存在しない到達パスを前提にし、(b) 実際のフィールド名は `change_note` ではなく `reason`（minLength:40、`artifact_commit` の `change_note` と混同したコピペミスと思われる）。plan の受け入れ基準文言より `schemas/tools.json`／設計書本文を正とすること。
- **Windows 環境で `node --test 'test/*.test.js'` が稀に `EPERM`/`errno:-4048`/`syscall:'rename'`（`atomic.js` の `writeAtomic` 内）で落ちることがある**: 並列実行される `node:test` ワーカー間のファイルリネーム競合と見られる一過性のフレーク。コード変更なしに同じコマンドを再実行して全件パスすれば回帰ではなくフレーク。
- **設計書 §19.4（下流 FROZEN の差し戻し）本文と §6.4.3（`artifact_commit` のエラー条件表）が矛盾していた**: §19.4 の地の文は `FROZEN` 状態での `artifact_commit`/`score_submit` を `E_STATE_VIOLATION`（detail.reason:"frozen_by_kickback"）と書くが、§6.4.3 自身の権威あるツール別エラー表は `E_FROZEN`／`E_SUPERSEDED` という専用コードを明記している。`src/errors/codes.js` にも両コードが `detailKeys` 付きで事前登録済み（T012 由来）だったため、T041 では専用コードを正とし `chain/supersede.js` は `E_FROZEN`/`E_SUPERSEDED` を投げる実装にした。地の文より各ツールの権威あるエラー表・`codes.js` の事前登録を優先すること。
- **`schemas/tools.json` の出力スキーマの `state` enum が `SUPERSEDED`/`FROZEN` を含んでいない**（`loop_open`/`loop_state`/`artifact_commit`/`score_submit`/`escalate` いずれの output.state enum も9状態中この2つが欠落。T012 時点の設計書抜粋がこの2状態を追加する前の版だった可能性）。ただし出力は実行時に `validate()` で検査されていない（`src/**` に output スキーマへの `validate()` 呼び出しは無い）ため実害は今のところ無いが、将来 output 検証を追加するタスクや schema 準拠テストを書くタスクは先にこの2状態を enum に足すこと。
- **T043 の `escalate(action:"kickback")` は `human_token` を実トークンと照合できない**: `human_token` の唯一の発行元は `escalation/token.js` の `createEscalation`（`request_human` で ESCALATED になったときだけ書かれる）。しかし `kickback` は FSM 上 `FINAL`/`FINAL_WITH_RELAXATION` から直接呼べる（`fsm/states.js` の `escalateOnly(['reopen','kickback'])`）一方、`FINAL` は `request_human` を許可しない（`escalateOnly` に含まれない）ため、その経路では絶対にペンディングトークンが存在しない。設計書 §14.3.2 A10「`human_token` は速度制限でありセキュリティ境界ではない」とも整合するので、`kickback` では `consumeToken()` を呼ばずスキーマの存在検査（`assertActionInputs`, minLength:16）だけで通す実装にした。実トークンとの照合は T044（MRTR）側の責務として先送りしている。
- **T045（`audit_export`）で、監査 JSON が要求する周ごとのメタデータがそれまでどこにも永続化されていなかった**: `artifact_commit` の入力（`change_note` / `addresses`）と算出した `diff` / `previous_digest` は、レスポンス（`buildEnvelope`）にしか現れず、`session.current_artifact` には `digest`/`bytes`/`committed_at` しか残らない。`rubric_amend` の入力 `reason`（minLength:40 必須）も `rubric_diff/<version>.json`（`saveRubricDiff`）に保存されていなかった。設計書 §12.2 の監査実例はこの両方を要求するため、T045 で `judge/round_store.js` に `rounds/<round>.commit.json`（`recordCommitForRound`/`readCommitForRound`）を追加し、`rubric/diff.js` の `saveRubricDiff` に `reason` を含めるよう拡張した。既存タスクが「レスポンスに出せば実装完了」で終わらせがちな箇所ほど、後続の監査/再検証タスクで永続化漏れが露見しやすい。
- **T031 の fileset スタブ（`E_VALIDATION`/`not_implemented`）を Phase 2（AT-*）着手時に解消した**: `artifact/fileset.js`（manifest検証・digest計算）・`implement/test_inventory.js`（R1-R3）・`implement/assert_mutation.js`（R5）は単体関数として存在済みだったが `artifact_commit.js` に未結線だった。結線時の要点: (1) fileset の `artifact.digest` は `saveContentArtifact` の「文字列丸ごとのsha256」ではなく `computeManifestDigest(files)`（`"<sha256>  <path>\n"` を path 昇順連結したものの sha256）なので、保存関数を分けて `saveFilesetArtifact(sDir, digest, manifest)` を新設し、`artifact/store.js` の `EXT_BY_KIND` に `fileset: '.manifest.json'` を追加して同じ `artifactPath()` 命名規則に載せた（`readArtifactContent` がそのまま fileset にも使え、`score_submit` の locator 根拠がマニフェストJSONテキストに対して素通しで効くようになる）。(2) 前周比較（test_inventory の R1-R3、テストファイル改変の R5）に必要な「前周の `files[]`/`test_inventory`」は `round_store` 経由では取りに行かず、`session.current_artifact` に `files`/`test_inventory` を持たせて次回コミット時に読む方式にした（round番号は score_submit 側でのみ進み、kickback/rebase で飛ぶこともあるため、round算術より session 直付けの方が単純で堅牢）。(3) diff のフィールド名は設計書本文の例（§19.5.3 の `modified_files`）ではなく `schemas/tools.json` の output スキーマ（`changed_files`）を権威として採用（既存のハマりポイントの「地の文より権威あるスキーマ優先」の原則をそのまま適用）。(4) `added_lines`/`removed_lines` は設計書どおり `null` を返すが、output スキーマ側は `type:"integer"` 必須（null非許容）のままで、実行時 `validate()` は output に対して呼ばれていないため実害なし（既存の「output enum欠落」の一件と同種の schema/設計書ズレ）。
- **`score_submit.js` は R4（`checkTestNotGreen`、`implement/test_inventory.js`）を一度も呼んでいなかった**: `artifact_commit.js` の fileset 結線（T031→本セッションで実施）で R1/R2/R3/R5 は commit 時点の検査として繋いだが、R4 は「テストが赤いまま auto 基準に pass_score 以上を付けたか」を**採点時点**でしか判定できないため `score_submit.js` 側に別途フックが要る。設計・実装済みの純粋関数はあった（`test_inventory.test.js` で単体テスト済み）が呼び出し元が無く、AT-15(d) 相当のシナリオは統合レベルでは常に無検査で通っていた。`score_submit.js` に `session.artifact_kind==='fileset' && session.current_artifact.test_inventory` のとき `criteriaById` から `verification==='auto'` な提出だけを抜き出して `checkTestNotGreen` を呼ぶよう追加。「既存の単体テストが緑」は「呼び出し元まで繋がっている」の証明にならない一例。
- **`escalate.js` の `action` は設計書 §4/§19.9.2 の FSM 定義（`FINAL`→`escalateOnly(['reopen','kickback'])`）に反して `'reopen'` を実装していない**: `escalate()` の action 分岐は `request_human`/`resolve`/`abort`/`rebase`/`kickback` の5つのみで、`reopen` は else 節の `E_INTERNAL`（`action not implemented in this build`）に落ちる。AT-12（上流を人間が直接 `reopen` して差し戻す想定）はこの経路をそのまま呼ぶと通らない。`rebase.test.js` が同種の未実装ギャップを `session.json` 直接書き換えで模す前例（`mutateDesignToV2`）を既に確立しているので、AT-12 のテストもこの前例に倣い session 直接編集で代替した。実装が必要になったら `chain/kickback.js` の `performKickback` を汎用化して upstream 側だけを動かす形（`from_session` を持たないシステム起票の reopen）にするのが自然。
- **`chain/budget.js` の `assertRoundBudget`（チェーン全体のラウンド予算）は `loop_open_create.js`（新規セッション作成時）にしか結線されておらず、`score_submit.js` の ITERATING 継続時（ラウンド消費のたび）には一切呼ばれない**。`grantExtraRounds`（`escalate(resolve, continue)` でチェーン全体に+6する関数）も呼び出し元がゼロで、`escalate.js` の `applyResolution('continue')` はセッション単体の `counters.extra_rounds_granted` だけを+3するローカルな仕組みで代替している（chain 単位の集計・上限とは無関係）。設計書 §13 AT-16（`score_submit` 実行中にチェーン予算超過を検知して `ESCALATED` に落とし、`escalate(continue)` でチェーン全体に+6する)はこの2点がどちらも未実装のため、そのままでは再現できない。AT-16 のテストは実際に結線されている `loop_open_create` 時点の `assertRoundBudget`（新規セッション作成がチェーン予算超過で拒否される経路）と `assertKickbackBudget`（既存, kickback.test.js で検証済み）に絞った。score_submit 時点でのチェーン予算超過検知を実装するタスクが将来来たら、この2関数への結線がまるごと必要になる。
- **`rubric_amend` で基準を削除しても、直前の `last_evaluation.must_fix[0]` はそのまま残る**: `artifact_commit.js` の `E_ADDRESS_MISSING` チェックは `session.last_evaluation.must_fix[0].criterion_id`（rubric_amend 前の評価結果）を見るため、その基準を rubric から削除した直後の commit でも `addresses` にその（もう rubric に存在しない）criterion_id を含めないと弾かれる。rubric_amend は `last_evaluation` を書き換えない。
- **`score_submit.js` は `checkEvidenceStale`（per-criterion, perCriterion.map 内）を `checkScoreInflation`（全criterion組み立て後）より先に評価する**: 成果物不変(digest同一)のままスコアを上げて `E_SCORE_INFLATION` を誘発したいテストで、根拠 excerpt を前周と一字一句同じにすると evidence_digest も前周と一致し `E_EVIDENCE_STALE` が先に飛んで狙った検査に届かない。inflation だけを単独で発火させたい場合は、excerpt の文言を前周と変える（本文中の別の抜き出し方にする等）こと。
- **AT系テストで1周に3点以上スコアを上げると `E_SCORE_JUMP` で落ちる**: `judge/anti_gaming.js` の `checkScoreJump` は `delta > max_score_jump`（既定3）のとき、`kind:'command'` かつ `exit_code===0` の evidence が2件以上ないと拒否する（design/plan モードでも `verifyCommandTargetDigest` は `loop_mode!=='implement'` なら無条件で通る＝`target_digest` 不要）。3〜4点の劇的な改善を1周で描きたいシナリオでは、locator evidence に加えて `exit_code:0` の command evidence を2件足すか、上げ幅を3以内に小分けする。
- **設計書 §13 AT-13 の `E_CHAIN_BUDGET_EXHAUSTED`（kickback）の例示 `detail:{check:"kickbacks", used:2, limit:2}` は実装と違う**: `src/chain/budget.js` の `assertKickbackBudget` は `used` ではなく `count` を返す（`assertRoundBudget` 側の同エラーも同様に `count`/`limit`）。また `kickback` action は FSM 上 `FINAL`/`FINAL_WITH_RELAXATION` からしか呼べないため（`escalateOnly(['reopen','kickback'])`）、chain_max_kickbacks 到達を確認するテストは毎回「kickback→(上流を直してrebase)→再finalizePlan」で下流をFINALに戻してから次のkickbackを呼ぶ必要がある（`kickback.test.js`の既存テストと同じ手順）。
- **設計書 §13 AT-16（チェーン予算の超過）の例が挙げる3点が `src/tools/score_submit.js`／`src/tools/escalate.js` のどこにも実装されていない**: (1) `score_submit` 自身が chain round 予算（`chain/budget.js` の `assertRoundBudget`）を検査して `verdict:"REVISE"`／`escalation:{reason:"chain_budget_exhausted"}` を返す経路、(2) `escalate(action:"resolve", resolution:"continue")` がチェーンレベルの `granted_extra_rounds` を上乗せする経路（`applyResolution` は `session.counters.extra_rounds_granted` というセッション単位の別カウンタしか触っておらず、`chain/budget.js` の `grantExtraRounds` はどこからも呼ばれていない）、(3) 予算超過でも合格なら `warnings:["chain_budget_exceeded"]` で押し通す経路。`grep -rn "chain_budget_exceeded\|chain_budget_exhausted\|REVISE" src/` は0件。実際に chain round 予算を検査する公開経路は `loop_open`（`plan`/`implement` 作成時、`loop_open_create.js` の `assertRoundBudget` 呼び出し）だけであり、`grantExtraRounds` は `test/chain_budget.test.js` が先例として直接呼んでいる契約専用関数。AT-16 はこの実態に合わせ、`loop_open` 経由の予算枯渇（複数セッションの round 合算）と `grantExtraRounds` の「1チェーン1回だけ」契約を module 直呼びで検証するに留めた。
- **`loop_state.js` の `buildUpstreamArtifact` にある「上流セッションが物理的に無ければ `warnings:['upstream_missing']` を積んで縮退する」分岐（AT-17 の設計書が期待する挙動）は現状デッドコード**: `loop_state`/`loop_open(resume)` はどちらも本体処理の先頭で `checkSupersede(dataDir, session, toolName)` を無条件に呼んでおり、`checkSupersede` は `session.upstream.session_id` のディレクトリが存在しない場合 `toolName` に関係なく（`ALWAYS_ALLOWED_TOOLS` の分岐より前に）即 `E_UPSTREAM_NOT_FOUND` を投げる。そのため `buildUpstreamArtifact` 内の `sessionExists` チェックに実行が到達することはない。設計書 §13 AT-17「上流が消えていた場合」の `orphan:true`／`state:"DRAFTING"` という優雅な縮退シナリオは未実装で、実際は `loop_open(resume)` も `loop_state` も upstream セッション削除後は即エラーになる。
- **`docs/design-rubric-loop-mcp.md` §19.13.2 のチェーン監査 JSON トップレベルは `additionalProperties:false` で `schema`/`$schema` キーを含まない**（セッション単位監査 v1 が持つ `schema` フィールドと非対称）。`rubric-loop-agy/`（未追跡の並行実装、T046 着手時に既に存在していた別実装。由来不明で本タスクの参照専用に留めた）はここに `schema`/`$schema` の両方を追加しており、この設計書のスキーマ定義に照らすと不適合。`rubric-loop/`（本実装）側の `chain_v2.js` は追加していない。設計書の JSON Schema 本文（`$id`/`required`/`properties`）が権威であり、他の実装や地の文の例（§19.13.3 など）はそれに従わせること。
- **`score_submit` で `verdict: 'ITERATING'` 時の `session.round += 1` より前に `scoredRound` を確定させないと次周の評価とテストインベントリ比較が壊れる**: `session.round += 1` を先に行うと、記録される `last_evaluation.round` が 1 周進んだ値（例: round 1 の評価なのに round 2）になり、次回周回時の `last_evaluation` 参照やテストインベントリ比較で不整合を起こす。必ずインクリメント前に `const scoredRound = session.round;` を保持すること。
- **Windows PowerShell で `node --test 'test/*.test.js'` のように単一引用符で glob を囲むと展開されず空実行になる**: Windows の shell は glob を自動展開せず node 側が展開するが、single quotes がリテラルとして残りマッチしない。`package.json` の test スクリプトは `node --test` のみにするか、クロスプラットフォーム対応の記述にすること。
- **`score_submit` の `E_EVIDENCE_REQUIRED`（`assertEvidenceRequired`、`src/evidence/verify.js`）は公開スキーマ経由では到達不能**: `schemas/tools.json` の `score_submit.input.properties.scores.items.properties.evidence` が `minItems:1` を強制するため、`evidence:[]` を送ると `validate()` が `E_VALIDATION`(`minItems 1`) を先に投げ、`assertEvidenceRequired` の空配列チェックには実行が到達しない。関数自身のコメントも「スキーマの minItems:1 に対する防御的な二重チェック」と自認している。T068/T069 で確立した「`TOOL_ERRORS` は設計書の総覧表ではなく実装の到達可能性を正とする」の原則を T070 でさらに一段掘り下げ、`score_submit` は 18 件ではなく 17 件が正（`E_EVIDENCE_REQUIRED` を除外）。`assertXxx` 系の防御的二重チェック関数をエラー網羅テストに組み込む際は、まず対応するスキーマ側に同じ制約（`minItems`/`minLength`/`required` 等）が既にあるか確認すること。
- **`normalizeForMatch`（`src/hash/digest.js`、locator 根拠の本文一致判定に使用）は改行を除去・畳み込みしない**: `\r\n→\n` の統一と行末空白の削除のみを行い、素の `\n` はそのまま残す。そのため成果物本文が `"文A\n文B"` のとき、テストの evidence `excerpt` を `"文A" + "文B"`（改行を挟まず連結した文字列）として組み立てると本文中に存在しない文字列になり `E_EVIDENCE_NOT_FOUND` で落ちる。複数文にまたがる根拠を新しい周のために作るときは、本文中の**改行を挟まない1文単位**でそのまま `excerpt` に使うこと（連結しない）。
- **`score_submit` の anti-gaming 系チェックは「スコアが前周より上がったときだけ」発火する**: `checkEvidenceStale`/`checkScoreJump` はどちらも `currentScore <= previousScore` なら即 return する（`src/judge/anti_gaming.js`）。そのため F7（停滞）のように前周と同スコアを出し続けるだけの再現テストでは、evidence の excerpt を前周から変えても変えなくても anti-gaming 系エラーは出ない。逆に F9（スコアジャンプ）を単独で再現したい場合、前周と同じ excerpt を使い回すと（スコアは上がっているので）`checkEvidenceStale` が `checkScoreJump` より先に飛ぶ（既存のハマりポイント「score_submit は checkEvidenceStale を checkScoreInflation より先に評価する」と同型の罠）。excerpt を新しい文に変えて stale 判定を素通りさせてから jump 判定に到達させること。
- **`artifact_commit.js` の content commit 経路（design/plan モード）は `src/artifact/plan_checks.js`（`checkPlan`）・`plan_schema.js`（`validatePlanSchema`/`parseParseContent`）・`design_refs.js` を一切呼んでいない**: `E_PLAN_SCHEMA`/`E_PLAN_INVALID`/`E_PLAN_DESIGN_REF` はこれらのモジュールの単体テスト（`plan_checks.test.js`/`plan_schema.test.js`/`design_refs.test.js`）では発火するが、公開ツール `artifactCommit()` からは構造的に到達不能（`grep -n "checkPlan\|PLAN_SCHEMA\|E_PLAN_DESIGN_REF" src/tools/artifact_commit.js` は0件）。単体テストが緑でも呼び出し元まで繋がっている証明にはならない別の一例（`score_submit.js` の R4 未結線と同型）。
- **`src/errors/codes.js` の `TOOL_ERRORS` レジストリが実際の到達可能性と食い違っていた（設計書 §19.6.7 の総覧表自体の誤りを転記したもの、KB-5 で修正済みの過去のカウント誤りと同系統の未検出ミス）**: 具体的には (1) `E_TEST_REGRESSION` は `score_submit.js` では一度も投げられず（`checkTestNotGreen` しか呼ばない）実際は `artifact_commit.js`（`checkTestInventoryOnCommit`）が投げる、(2) `score_submit.js`/`artifact_commit.js`/`rubric_amend.js`/`escalate.js` は全て明示的に `E_SESSION_NOT_FOUND` を投げているのにそれぞれの登録リストから漏れていた、(3) `checkSupersede` は対象ツールが `ALWAYS_ALLOWED_TOOLS=['loop_open','loop_state','escalate','audit_export']` に含まれない場合のみ `E_FROZEN`/`E_SUPERSEDED` で止まるが、その手前の `E_UPSTREAM_NOT_FOUND` は全ツール共通で無条件に投げるため `score_submit`/`rubric_amend` からも到達可能なのに未登録だった、(4) `score_submit.js` は `E_UPSTREAM_NOT_ALLOWED`（design モード限定・design は score_submit 未使用）も `E_CHAIN_BUDGET_EXHAUSTED`（`assertRoundBudget` を score_submit は呼ばない、既存ハマりポイントのAT-16と同根）もコード上一度も投げない。T068 でレジストリを実装の実態に合わせて修正した（`artifact_commit`: `E_PLAN_SCHEMA`/`E_PLAN_INVALID`/`E_PLAN_DESIGN_REF` を外し `E_SESSION_NOT_FOUND`/`E_TEST_REGRESSION`/`E_UPSTREAM_NOT_FOUND` を追加＝13件のまま; `score_submit`: `E_TEST_REGRESSION`/`E_UPSTREAM_NOT_ALLOWED`/`E_CHAIN_BUDGET_EXHAUSTED` を外し `E_SESSION_NOT_FOUND`/`E_UPSTREAM_NOT_FOUND` を追加＝19→18件; `rubric_amend`: 構造的に到達不能な `E_THRESHOLD_IMMUTABLE` を外し `E_SESSION_NOT_FOUND`/`E_UPSTREAM_NOT_FOUND`/`E_FROZEN`/`E_SUPERSEDED` を追加＝5→8件; `escalate`: `E_SESSION_NOT_FOUND` を追加＝7→8件）。設計書 §19.6.7 本文の数字より、各ツールの実装（`fail()`呼び出し・呼び出しグラフ）を正とすること。
- **`rubric-loop/server/main.js` に `tools/call` と標準 `initialize` ハンドラが未登録だった**: `server/main.js` は `server/discover` と `tools/list` のみ登録しており、外部 MCP クライアント（Claude Code 等）から stdio で起動した際に `initialize` や `tools/call` が `Method not found (-32601)` で落ちていた。`src/mcp/initialize.js` と `src/mcp/tools_call.js` を追加して router に登録し解決。
- **`artifact_commit.js` に `parsePlanContent` / `checkPlan` / `checkDesignRefs` が結線されたため、plan モードの成果物は厳格な JSON 必須となった**: 以前のハマりポイントで「plan content 検証は未結線」と記録されていたが、結線後は plan モードで raw Markdown をコミットすると `E_PLAN_SCHEMA: content is not valid JSON` で即落ちする。`plan_version: 1`, `summary` (minLength: 40), `tasks[]`, `design_refs` (上流本文中の見出し等と完全一致) を含む有効な JSON 文字列を渡し、locator evidence の `excerpt` にも改行を含まない要約文（`summary` 等）を指定すること。
- **`src/mcp/tools_list.js` の `handleToolsList()` が `{name}` のみを返し `description`/`inputSchema` を欠いていたため、MCP接続が"Connected"と表示されていても外部クライアント（Claude Codeの`tool_search_tool_regex`含む）側でtoolが一切登録されず検索にヒットしなかった**: `initialize`/`tools/list` を直接stdioで叩くとサーバー自体は正常応答していた（7ツール名は返る）ため、接続断ではなく応答の中身が原因と分かった。MCP仕様上クライアントがtoolを登録するには`inputSchema`（JSON Schema）が要る。`schemas/tools.json`の`tools[name].input`をそのまま`inputSchema`に、ハードコードした短文を`description`に補って解決（`TOOL_SCHEMAS`を`tools_list.js`にimportして`TOOL_DESCRIPTIONS`マップと合成）。同様の症状（"Connected"なのにtool_searchで0件）が出たら、まずMCP接続そのものではなく`tools/list`の応答形状（name以外のフィールドの有無）を疑うこと。
<!-- knowledge-kit:end section=gotchas -->
