「Sliding Window Counter 方式の Rate Limiter」について、仕様策定から実装計画立案、そして実装・検証までを一気通貫で完遂してください。

以下のステップを順序通りに進めて各成果物ファイルを作成してください：

1. 【フェーズ1: 仕様策定】
   - `specification.md` を作成し、APIインターフェース、ミリ秒境界計算式、エッジケース、エラーコードを定義する
2. 【フェーズ2: 実装計画】
   - `implementation_plan.md` を作成し、TDDに基づく段階的タスク分割と各タスクの受け入れ基準を定義する
3. 【フェーズ3: 実装とテスト】
   - `src/rate_limiter.js` にコードを実装し、`test/rate_limiter.test.js` に単体テストを作成する
   - `node --test test/rate_limiter.test.js` を実行し、全件合格（PASS）を確認する

※ 各フェーズを省略して直接コードを書いたり、仕様のエッジケースを実装段階で間引くことは認められません。
※ テストのアサーション緩和・無効化や常時合格するモック実装は禁止します。
